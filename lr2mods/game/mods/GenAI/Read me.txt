Thanks for checking out my Gen AI mod for lab rats 2.

Drop the GenAI folder in the "game/Mods" folder.
Check the stable-diffusion-guide for help in setting up stable diffusion.

V0.3
-Added auto generate on change toggle
-Implemented async image generation with progress percent
-Fixed issue with colour picking
-Generate image icons set to hidden when auto generate is on
-Moved generate actions to the top of the image area
-Separated prompt building to it's own script for easier modability

V0.2
-Allow for changing base width/height (adust position so it fits on screen, and maintain aspect ratio for fullscreen mode)
-Add override option for negative prompt
-Add section where you can add always append positive/negative prompt (can use for styles or lora's)
-Section where you can edit style part of the prompt
-Separated hr parts of payload if high res fix is off
-Add per person section where you can append to the positive prompt
-Text input for setting adetailer model name
-Fixed persistant issue with saving config settings
-Fixed serialization issue preventing saving images between sessions
-Added clothing color to prompt
-Fix for inserting mod into an existing save game

V0.1
Initial

--medvram --no-half-vae --listen --port=7860 --api --cors-allow-origins *